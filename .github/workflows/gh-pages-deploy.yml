name: Deploy on github pages

on:
  push:
    branches: [ master ]
  
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Installing modules
        run: npm ci
      
      - name: Deploying
        run: ng deploy
